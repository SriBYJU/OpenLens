import {useMemo,useState} from 'react';import Page from '../components/Page';import Glasses from '../components/Glasses';import {capabilityKeys} from '../core';import {devices} from '../data/devices';
export default function Devices(){const [query,setQuery]=useState('');const [selected,setSelected]=useState<string[]>(['openlens-twin','brilliant-frame']);const [differences,setDifferences]=useState(false);const filtered=devices.filter(d=>`${d.name} ${d.manufacturer} ${d.summary}`.toLowerCase().includes(query.toLowerCase()));const compare=devices.filter(d=>selected.includes(d.id));const rows=useMemo(()=>capabilityKeys.filter(key=>!differences||new Set(compare.map(d=>`${d.capabilities[key].physical}/${d.capabilities[key].manufacturerAccess}`)).size>1),[compare,differences]);const toggle=(id:string)=>setSelected(current=>current.includes(id)?current.filter(v=>v!==id):current.length<3?[...current,id]:[...current.slice(1),id]);return <Page index="02" eyebrow="DEVICE UNIVERSE / EVIDENCE-AWARE CATALOG" title={<>The field,<br/><em>without the fog.</em></>} lead="Inspect the physical claim, manufacturer access and OpenLens integration level as separate facts. Select up to three profiles for a side-by-side capability matrix."><div className="catalog-tools"><input className="search-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search device, maker or capability…"/><label className="toggle"><input type="checkbox" checked={differences} onChange={e=>setDifferences(e.target.checked)}/><span/>Differences only</label></div><div className="device-catalog">{filtered.map((device,index)=><article className={`device-record ${selected.includes(device.id)?'selected':''}`} key={device.id}><button className="compare-toggle" onClick={()=>toggle(device.id)} aria-pressed={selected.includes(device.id)}>{selected.includes(device.id)?'IN COMPARISON':'ADD TO COMPARE'}</button><div className="device-render"><span>0{index+1}</span><Glasses compact={index%2===1}/></div><div className="device-info"><div><p className="eyebrow">{device.manufacturer}</p><h2>{device.name}</h2></div><span className={`truth-label ${device.integrationStatus}`}>{device.integrationStatus.replaceAll('-',' ')}</span><p>{device.summary}</p><div className="device-meta"><span>{device.capabilities.display.physical==='present'?(device.optics.horizontalFovDegrees?.value?`${device.optics.horizontalFovDegrees.value}° FOV`:'FOV UNKNOWN'):'NO DISPLAY'}</span><span>REV {device.revision}</span><span>{device.sources.length} SOURCES</span></div><div className="button-row"><a className="button" href={device.kind==='digital-twin'?`#/lab?device=${device.id}`:`#/research?device=${device.id}`}>{device.kind==='digital-twin'?'Open twin':'Inspect evidence'}</a></div></div></article>)}</div><section className="compare-table"><header><div><p className="eyebrow">CAPABILITY MATRIX</p><h2>{compare.length} profiles in view</h2></div><span>PHYSICAL / MANUFACTURER API</span></header><div className="table-scroll"><table><thead><tr><th>Capability</th>{compare.map(d=><th key={d.id}>{d.name}<small>{d.integrationStatus}</small></th>)}</tr></thead><tbody>{rows.map(key=><tr key={key}><th>{key}</th>{compare.map(d=><td key={d.id}><b>{d.capabilities[key].physical}</b><small>{d.capabilities[key].manufacturerAccess}</small></td>)}</tr>)}<tr><th>OpenLens adapter</th>{compare.map(d=><td key={d.id}><b>{d.integrationStatus}</b><small>{d.kind==='research'?'not executable':'registered twin'}</small></td>)}</tr></tbody></table></div></section></Page>}
