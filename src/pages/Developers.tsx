import Page from '../components/Page';
const example=`import { compileExperience, getDevice, simulate } from "@openlens/core";\n\nconst device = getDevice("openlens-twin");\nconst plan = compileExperience(experience, device);\n\nif (plan.compatibility !== "blocked") {\n  const artifact = simulate(plan, scenario);\n  console.log(artifact.trace);\n}`;
export default function Developers(){return <Page index="06" eyebrow="DEVELOPERS / PUBLIC CONTRACT" title={<>Build through<br/><em>the differences.</em></>} lead="The engine exposes small typed boundaries for devices, adapters, experiences, runs and benchmark artifacts. The browser demo works without keys."><div className="developer-grid"><section className="code-stage"><header><span>src/core/example.ts</span><i>TYPE-SAFE</i></header><pre>{example}</pre></section><div className="developer-copy"><p className="eyebrow">ONE CONTINUOUS ARTIFACT</p><h2>Author → compile → run → trace → benchmark.</h2><p>The same plan fingerprint moves through every system. Device revisions, adapter disclosure, catalog version, methodology and raw trace stay attached.</p><a className="button primary" href="#/compiler">Open the compiler</a></div></div><div className="contract-grid"><article><span>01</span><h3>Device profile</h3><p>Evidence-aware physical capability and manufacturer access claims.</p></article><article><span>02</span><h3>Adapter manifest</h3><p>The executable integration state, declared independently from hardware evidence.</p></article><article><span>03</span><h3>Experience IR</h3><p>Requirements, ordered outputs, fallback, privacy and cost boundaries.</p></article><article><span>04</span><h3>Run artifact</h3><p>Versioned snapshots, scenario inputs, status, output and canonical spans.</p></article></div><section className="api-strip"><div><p className="eyebrow">OPEN SOURCE</p><h2>Read the system, then change it.</h2></div><a className="button" href="https://github.com/SriBYJU/OpenLens" target="_blank" rel="noreferrer">View repository ↗</a></section></Page>}
